{{- if ne .chezmoi.hostname "orchid" -}}
#
# DEPRECTAED: use wayland/sway instead
#
{{ end -}}
# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!
#
# This config file uses keycodes (bindsym) and was written for the QWERTZ
# layout.
#
# To get a config file with the same key positions, but for your current
# layout, use the i3-config-wizard
#

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango: InconsolataLGC Nerd Font 11

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# Start XDG autostart .desktop files using dex. See also
# https://wiki.archlinux.org/index.php/XDG_Autostart
exec --no-startup-id dex --autostart --environment i3

# Use feh to set a wallpaper
exec --no-startup-id feh --bg-scale --no-fehbg /home/jonas/style/ioki-style/ioki-wallpaper-neon.png

# Use pactl to adjust volume in PulseAudio.
set $defsink alsa_output.pci-0000_00_1f.3.analog-stereo
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-mute $defsink false; exec --no-startup-id pactl set-sink-volume $defsink +5% #increase sound volume
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-mute $defsink false; exec --no-startup-id pactl set-sink-volume $defsink -5% #decrease sound volume
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute $defsink toggle
bindsym XF86AudioMicMute exec --no-startup-id amixer set Capture toggle # mute Mic

# use these keys for focus, movement, and resize directions when reaching for
# the arrows is not convenient
set $up l
set $down k
set $left j
set $right semicolon

# define modifer keys (Mod4 = WIN, Mod1 = Alt)
set $mod Mod4
set $mymod Mod1

# use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# move tiling windows via drag & drop by left-clicking into the title bar,
# or left-clicking anywhere into the window while holding the floating modifier.
tiling_drag modifier titlebar

# start a terminal
bindsym $mod+Return exec i3-sensible-terminal

# kill focused window
bindsym $mod+Shift+q kill

# start dmenu (a program launcher)
#bindsym $mod+d exec --no-startup-id dmenu_run
# A more modern dmenu replacement is rofi:
bindsym $mod+d exec "rofi -modi drun,run -show drun"
# There also is i3-dmenu-desktop which only displays applications shipping a
# .desktop file. It is a wrapper around dmenu, so you need that installed.
# bindsym $mod+d exec --no-startup-id i3-dmenu-desktop
bindsym $mod+$mymod+space exec --no-startup-id i3-dmenu-desktop --dmenu="rofi -dmenu -i -p"

# change focus
bindsym $mod+$left focus left
bindsym $mod+$down focus down
bindsym $mod+$up focus up
bindsym $mod+$right focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+$left move left
bindsym $mod+Shift+$down move down
bindsym $mod+Shift+$up move up
bindsym $mod+Shift+$right move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+h split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

# move the currently focused window to the scratchpad
bindsym $mod+Shift+minus move scratchpad

# Show the next scratchpad window or hide the focused scratchpad window.
# If there are multiple scratchpad windows, this command cycles through them.
bindsym $mod+minus scratchpad show

# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $ws1 "1: www"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4: notes"
set $ws5 "5: IDE"
set $ws6 "6: RDP"
set $ws7 "7: GK"
set $ws8 "8: tasks"
set $ws9 "9: chat"
set $ws10 "10"

# short names for screens
set $int "eDP-1"
set $ext "DP-2-2"

# switch to workspace
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10

# enable toggle back to latest WS:
workspace_auto_back_and_forth yes

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10

# Move workspace to screen
bindsym $mod+$mymod+1 move workspace to output $int
bindsym $mod+$mymod+2 move workspace to output $ext
#bindsym $mod+$mymod+3 move workspace to output $extr
bindsym $mod+$mymod+Left move workspace to output left
bindsym $mod+$mymod+Down move workspace to output down
bindsym $mod+$mymod+Up move workspace to output up
bindsym $mod+$mymod+Right move workspace to output right



# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym $left       resize shrink width 10 px or 10 ppt
        bindsym $down       resize grow height 10 px or 10 ppt
        bindsym $up         resize shrink height 10 px or 10 ppt
        bindsym $right      resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left        resize shrink width 10 px or 10 ppt
        bindsym Down        resize grow height 10 px or 10 ppt
        bindsym Up          resize shrink height 10 px or 10 ppt
        bindsym Right       resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

bindsym $mod+r mode "resize"

### Color Settings for Windows
# class                 border  bground text    indicator child_border
client.focused          #b6defb #a9cdeb #383C4A #0000FF   #383C4A
client.focused_inactive #b6defb #383C4A #F2F2F2 #000099   #383C4A
client.unfocused        #383C4A #383C4A #F2F2F2 #000099   #383C4A
client.urgent           #F99286 #F99286 #383C4A #FF0000   #F99286
client.placeholder      #000000 #000000 #F2F2F2 #000000   #000000

client.background       #F2F2F2

### Gaps
gaps inner 8px
gaps outer 0px
smart_gaps off

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
    font pango:Inconsolata, FontAwesome 10
    position bottom
    tray_output primary
    tray_padding 4 px
    status_command i3status-rs /home/jonas/.config/i3status-rs/config.toml
    colors {
        background #4b5162
        focused_workspace #a9cdeb #a9cdeb #383c4a
        active_workspace #a9cdeb #383c4a #f2f2f2
        inactive_workspace #383c4a #383c4a #f2f2f2
        urgent_workspace #f99286 #f99286 #383c4a
    }
}


# open rofi frontend to pass
bindsym $mod+p exec --no-startup-id rofi-pass

# open rofi frontend to taskwarrior
bindsym $mod+t exec --no-startup-id ~/bin/tw-rofi

# launch rofimoji (currently in native mode for direct typing support, see docs for options/alternatives)
bindsym $mymod+e exec --no-startup-id rofimoji
#rofi mode variatn, but cannot type directly...: rofi -modi "emoji:rofimoji" -show emoji

# System Exit
#bindsym $mod+$mymod+x exec --no-startup-id ~/bin/bye-dmenu.py -l 10
bindsym $mod+$mymod+x exec --no-startup-id ~/bin/bye-rofi

# Screen Layout controls
#bindsym $mod+$mymod+d exec --no-startup-id ~/bin/display-dmenu.py -l 10
bindsym $mod+$mymod+a exec --no-startup-id ~/.screenlayout/home.sh
bindsym $mod+$mymod+s exec --no-startup-id ~/.screenlayout/undocked.sh
#bindsym $mod+$mymod+d exec --no-startup-id ~/.screenlayout/docked.sh #setup for dual ext screen (office old style)
bindsym $mod+$mymod+w exec --no-startup-id ~/.screenlayout/hdmi-office-standalone.sh
bindsym $mod+$mymod+h exec --no-startup-id ~/.screenlayout/hdmi-4k.sh

# Screen Lock -- for plain bg color use the following instead: -c 373b41
bindsym $mod+$mymod+y exec --no-startup-id i3lock -e -c 373b41 -p default

## For a screen lock with an image I will likely need some custom scaling/tiling script.
## This does not create the desired result: -t -i /home/jonas/stuff/ioki-style/ioki-wallpaper-neon.png

# Screenshots: [p] full virt screen | [o] window | [l] interactive rect | [k] interactive rect to clipboard
bindsym $mod+$mymod+p exec --no-startup-id maim --noopengl ~/screenshots/$(date +%F-%H%M%S)-maim-desktop.png
bindsym $mod+$mymod+o exec --no-startup-id maim --noopengl --window=$(xdotool getactivewindow) ~/screenshots/$(date +%F-%H%M%S)-maim-window.png
bindsym $mod+$mymod+l exec --no-startup-id maim --noopengl --select ~/screenshots/$(date +%F-%H%M%S)-maim-selection.png
bindsym $mod+$mymod+k exec --no-startup-id maim --noopengl --select | xclip -selection clipboard -t image/png -i

# Internal Display brightness FIXME: fix the issue "No outputs have backlight property" first, see wiki
#bindsym XF86MonBrightnessUp exec xbacklight -inc 10 # increase screen brightness
#bindsym XF86MonBrightnessDown exec xbacklight -dec 10 # decrease screen brightness

# Media player controls (this assumes that the play key always sends play, so toggle)
#bindsym XF86AudioPlay exec --no-startup-id playerctl play-pause
#bindsym XF86AudioNext exec --no-startup-id playerctl next
#bindsym XF86AudioPrev exec --no-startup-id playerctl previous

# Assign workspaces:
assign [class="firefox"] $ws1
assign [class="obsidian"] $ws4
assign [class="jetbrains-idea-ce"] $ws5
assign [class="jetbrains-idea"] $ws5
assign [class="jetbrains-idea-ce" title="Tip of the Day"] $ws5
assign [class="jetbrains-idea" title="Tip of the Day"] $ws5
assign [class="jetbrains-idea-ce" title="Welcome to IntelliJ IDEA"] $ws5
assign [class="jetbrains-idea" title="Welcome to IntelliJ IDEA"] $ws5
assign [class="org.remmina.Remmina"] $ws6
assign [class="GitKraken"] $ws7
assign [class="Slack"] $ws9
assign [class="Keybase"] $ws9
#assign [class="Thunderbird"] $ws10

# Fix floating status
for_window [title="Thunderbird Preferences"] floating enable
for_window [title="Volume Control"] floating enable
for_window [title="Bluetooth Devices"] floating enable
for_window [title="Remmina Remote Desktop Client"] floating enable
for_window [title="Microsoft Teams Notification"] floating enable

# This is used to open a floating terminal in certain places, make sure the launching command
# sets the urxvt instance name correctly with [-name "floating_terminal"], and ideally also sets a geometry!
for_window [class="URxvt" instance="floating_terminal$"] floating enable
